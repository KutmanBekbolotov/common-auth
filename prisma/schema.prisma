generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum UserRole {
  admin
  ceo
  license
  spec
  hr
  ovk
  TV
  Practice
  Terminal
  SuperAdmin
  INVENTORY_IT
  INVENTORY_AHO
  INVENTORY_ACCOUNTANT
  INVENTORY_AUDITOR
  Manager
  Auditor
  Operator
  System
  PRESSA
  GeneralDepartment    @map("General-department")
  Citizen
  citizen
}

enum ScopeOptionType {
  orgId
  departmentId
}

model User {
  id                String   @id @default(uuid())
  email             String   @unique
  passwordHash      String
  refreshTokenHash  String?  @unique @map("refresh_token_hash")
  sessionId         String?  @map("session_id")
  role              UserRole
  username          String?
  phone             String?
  pin               String?
  orgId             String?
  departmentId      String?
  position          String?
  photoUrl          String?
  legacyFirebaseUid String?  @unique
  disabled          Boolean  @default(false)
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([role])
  @@index([orgId])
  @@index([departmentId])
  @@index([position])
  @@index([sessionId])
  @@map("users")
}

model ScopeOption {
  id        String          @id @default(uuid())
  type      ScopeOptionType
  value     String
  createdAt DateTime        @default(now())
  updatedAt DateTime        @updatedAt

  @@unique([type, value])
  @@index([type])
  @@map("scope_options")
}
